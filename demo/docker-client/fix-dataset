#!/bin/bash

I=0
for x in $(ls /datasets/flickr_imgs/*.jpg); do
    mv $x /datasets/flickr_imgs/im"$I".jpg
    I=$((I+1))
done
T=0
for x in $(ls /datasets/flickr_tags/*.txt); do
    mv $x /datasets/flickr_tags/tags"$T".txt
    T=$((T+1))
done